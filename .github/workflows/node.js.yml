name: Node.js CI

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: self-hosted

    strategy:
      matrix:
        node-version: [18.x]
    defaults:
        run:
          working-directory: backend
    steps:
    - uses: actions/checkout@v3

    - name: Set up Node.js

      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        cache-dependency-path: backend/package-lock.json

    - name: Install and test in the backend directory
      working-directory: backend
      run: |
        npm ci
        touch .env
        echo "${{ secrets.PROD_ENV_FILE }}" > .env
    - name: Restart the server
    - run:  pm2 restart BackendAPI  


    # Add more steps or jobs for deployment or additional testing if needed
